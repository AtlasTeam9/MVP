services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:${BACKEND_PORT:-8000}/api/v1}
    init: true
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
